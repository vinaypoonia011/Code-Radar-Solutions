#include <stdio.h>

int main() {
    int a;
    
    // Taking input
    scanf("%d %d", &a, &b);
    
    // Checking if numbers are equal
    if (a == b) {
        printf("True\n");
    } else {
        printf("False\n");
    }

    return 0;
}